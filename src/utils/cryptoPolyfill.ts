import 'react-native-get-random-values';

// This file serves as a polyfill for the crypto.getRandomValues API
// that the uuid library requires. The import above adds support for 
// getRandomValues in React Native environments. 